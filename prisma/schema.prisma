// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  // output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Promo {
  id        Int      @id @default(autoincrement())
  libelle   String   @unique
  dateDebut DateTime
  dateFin   DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// model Competence {
//   id        Int      @id @default(autoincrement())
//   libelle   String   @unique
//   niveau    Niveau[]
// }


// model Niveau {
//   id            Int        @id @default(autoincrement())
//   niveau        Int
//   competenceId  Int
//   competence    Competence @relation(fields: [competenceId], references: [id])
// }

model Niveau {
  id          Int          @id @default(autoincrement())
  libellet    String       @unique
  rang        Int
  competences Competence[] // relation many-to-many implicite
}

model Competence {
  id           Int       @id @default(autoincrement())
  libelle      String
  niveaux      Niveau[]  // relation many-to-many implicite
  referentiels Referentiel[] @relation("ReferentielCompetences")
}

model Referentiel {
  id           Int           @id @default(autoincrement())
  libelle      String
  competences  Competence[]  @relation("ReferentielCompetences")
}






